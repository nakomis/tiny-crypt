[env:attiny85]
platform = atmelavr
board = attiny85
framework = arduino

; 16 MHz required for V-USB — use external crystal or PLL trick
board_build.f_cpu = 16000000L

; Programme via an ESP32/Arduino running the ArduinoISP sketch
upload_protocol = arduinoisp
