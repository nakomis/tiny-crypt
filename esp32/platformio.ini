[env:esp32dev]
platform = espressif32
board = az-delivery-devkit-v4
framework = arduino
monitor_speed = 115200

; I2C pins (default ESP32 SDA=21, SCL=22 — change if using custom wiring)
build_flags =
    -D SDA_PIN=21
    -D SCL_PIN=22
